import { motion } from "framer-motion";

const AsideCard = () => {
    return (
        <motion.aside className="fixed w-[320px] h-screen top-0 right-0 z-20 bg-[#1c1c1c]"></motion.aside>
    );
};

export default AsideCard;
